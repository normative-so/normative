{
  "name": "consumer",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.mjs",
  "scripts": {
    "start": "NODE_OPTIONS='--dns-result-order=ipv4first' node .",
    "start:watch": "NODE_OPTIONS='--dns-result-order=ipv4first' nodemon --inspect=0.0.0.0:9231 .",
    "build": "tsc || exit 0",
    "build:watch": "tsc --watch --preserveWatchOutput",
    "dev": "NODE_OPTIONS='--dns-result-order=ipv4first' concurrently npm:build:watch pnpm:start:watch",
    "prod": "npm run build && npm run start",
    "test": "mocha"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.7.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "bullmq": "^5.21.1",
    "concurrently": "^8.2.2",
    "ioredis": "^5.4.1",
    "nodemon": "^3.1.0"
  }
}
